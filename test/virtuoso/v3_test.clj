(ns virtuoso.v3-test
  (:require
   [clojure.test :refer [deftest is]]
   [virtuoso.v3 :as v]))

;; with-executor
;; future-via
;; overrides
;; bindings?
;; thread
;; future
;; map
;; fmap
;; pmap capture evaluation
;; pvalues
